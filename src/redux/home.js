// const INCREMENT = 'redux-example/counter/INCREMENT';

const initialState = {
  home: 0,
  list: [
    'JavaScript',
    'CSS',
    'HTML'
  ]
};

export default (state = initialState, action = {}) => {
  // switch (action.type) {
  //   case INCREMENT:
  //     const {count} = state;
  //     return {
  //       count: count + 1
  //     };
  //   default:
      return state;
  // }
}
